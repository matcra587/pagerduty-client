---
name: ci

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  go-test:
    name: Go test
    # clover: provider=auto tags=actions
    uses: matcra587/github-actions/.github/workflows/go-test.yml@8b104684e72bef79fca78b294accb5f789d3f202
    permissions:
      contents: read

  go-lint:
    name: Go lint
    # clover: provider=auto tags=actions
    uses: matcra587/github-actions/.github/workflows/go-lint.yml@8b104684e72bef79fca78b294accb5f789d3f202
    permissions:
      contents: read

  md-lint:
    name: Markdown lint
    # clover: provider=auto tags=actions
    uses: matcra587/github-actions/.github/workflows/md-lint.yml@1f771927fa94bbb42fee46581c0cf6676080519e
    permissions:
      contents: read
